# INSTALL XBINDKEYS AND SXKHD

alt+Return
    st
XF86AudioMute
    pactl set-sink-mute 0 toggle
XF86AudioPlay
    playerctl play-pause
XF86AudioNext
    playerctl next
XF86AudioPrev
    playerctl previous
XF86AudioRaiseVolume
    amixer -D pulse sset Master 5%+
XF86AudioLowerVolume
    amixer -D pulse sset Master 5%-
XF86MonBrightnessUp
    xbacklight -inc 5 # increase screen brightness
alt+v
    xbacklight -inc 5 # increase screen brightness
XF86MonBrightnessDown
    xbacklight -dec 5 # decrease screen brightness
alt+c
    xbacklight -dec 5 # decrease screen brightness
Print
    maim $HOME/Pictures/$(date +"%Y-%m-%d_%H-%M-%S").png
shift+Print
    maim -o --nodecorations 2 -s $HOME/Pictures/$(date +"%Y-%m-%d_%H-%M-%S").png
Caps_Lock
    ~/.local/bin/scripts/tools/caps_state.sh && ~/.local/bin/scripts/tools/caps_state.sh && sleep 50
alt+shift+l
    ~/.local/bin/scripts/commands/kblyt
alt+shift+u 
	setxkbmap -layout 'us' && xset r rate 240 50 && notify-send "lng english"
alt+shift+a 
	setxkbmap -layout 'am' -variant 'phonetic' && xset r rate 240 50 && notify-send "lng armenian"
alt+shift+r
	setxkbmap -layout 'ru' -variant 'phonetic' && xset r rate 240 50 && notify-send "lng russian"
alt+shift+p
    ~/.local/bin/scripts/commands/power
XF86PowerOff
    ~/.local/bin/scripts/commands/power
alt+x
    ~/.local/bin/scripts/commands/scr
alt+o
    rofi -show drun -columns 2 -show-icons -padding 18 -modi drun -theme ~/.cache/wal/colors-rofi-dark.rasi
alt+r
    xfce4-terminal -e "cava" --hide-borders --hide-toolbar --hide-menubar --hide-scrollbar --title=desktopconsole --geometry=130x44+0+0 &
alt+a
    ~/.local/bin/scripts/tools/cabl
alt+d
    ~/.local/bin/scripts/tools/tools
button8
    xte "keydown Alt_R" "keydown Tab" "keyup Alt_R" "keyup Tab" "sleep 1"
button9
    playerctl play-pause
alt+e
    dmenuunicode
alt+{F9,F10}
	{dmenumount, dmenuumount}
