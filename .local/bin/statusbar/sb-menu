#!/bin/sh
source sb-essentials

case $BLOCK_BUTTON in
    1) ~/.config/rofi/launchers/misc/launcher.sh ;;
	# 4) xbacklight -inc 10 ;;
	# 5) xbacklight -dec 10 ;;
	6) POPUP_TERMINAL -e "$EDITOR" "$0" ;;
esac

echo " "
