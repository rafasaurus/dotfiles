#!/bin/sh

[ -f ~/.wprofile ] && . ~/.wprofile

export XDG_RUNTIME_DIR=/tmp/xdg-runtime-$(id -u)
export XCURSOR_THEME="dwlcursor"
export XCURSOR_SIZE=29
mkdir -p $XDG_RUNTIME_DIR

dbus-run-session dwl -s 'wlinit.sh' 2>&1 | tee /tmp/dwl.log
