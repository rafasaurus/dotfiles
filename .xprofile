#!/bin/sh

# This file runs when a DM logs you into a graphical session.
# If you use startx/xinit like a Chad, this file will also be sourced.

# feh --bg-scale ~/wallpaper/wall_5.jpg # setting background
feh --bg-scale ~/Dropbox/wallpaper/705837.png
# compton --config ~/.config/compton/config --vsync opengl --mark-wmwin-focused --mark-ovredir-focused -b --xrender-sync --xrender-sync-fence # for window blur effect
compton -CGb
slstatus &		# script for updating the status bar
dunst &			# dunst for notifications
logid &
xset r rate 300 50 &	# Speed xrate up
xmodmap $HOME/.Xmodmap &
unclutter &		# Remove mouse when idle
xrdb ~/.Xresources # update x database
nordvpn connect georgia &
i8kmon &
bluetoothctl power on &
i8kfan - 0 &
sxhkd &			# Bind keys with sxhkd
dropbox &

xrandr --dpi 115 &
if type chromium >/dev/null 2>/dev/null
then
	export BROWSER=chromium
fi

export BROWSER=chromium
notify-send "Welcome to Rice of RAFASAURUS"
